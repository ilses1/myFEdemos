# TODO - PDF2 预览后续建议

## 1. 优化建议

- **检测插件支持**: 虽然现代浏览器都支持 PDF 预览，但可以增加一个简单的检测逻辑，如果 `<iframe>` 加载失败（例如在某些极旧或特殊的移动端浏览器上），提示用户下载。
- **暗黑模式适配**: 浏览器原生的 PDF 预览插件通常有自己的 UI 风格，可能不会完全跟随项目的主题色切换。如果对 UI 一致性要求极高，建议继续使用 `pdf` (第三方组件) 模式。

## 2. 操作指引

- 访问 `/upload/pdf2` 即可测试功能。
