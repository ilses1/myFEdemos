## 待办事项

- 是否需要将 pdfjs worker 改为本地静态资源以支持离线
- 是否需要增加 PDF 预览失败的详细错误提示与重试入口
- 是否需要引入测试框架（如 Vitest + RTL）覆盖 PDF 预览

## 处理建议

- 若希望自托管 worker，可将 pdf.worker.min.js 放入 public/ 并改为本地路径
- 若需要更强的交互（批注、签名），需重新评估库选型
