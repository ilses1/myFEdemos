# TODO - PDF3 预览后续待办

## 1. 性能优化

- [ ] **缩略图虚拟列表**: 当 PDF 超过 100 页时，左侧侧边栏建议引入虚拟列表技术减少 DOM 节点。
- [ ] **自托管 Worker**: 将 `pdfjs-dist` 的 worker 文件下载至 `public/` 目录，避免对外部 CDN 的依赖。

## 2. 功能增强

- [ ] **缩略图大小调节**: 支持用户通过拖拽侧边栏边界来调整缩略图显示大小。
- [ ] **双向滚动联动**: 目前主视图滚动会高亮缩略图，后续可增加“缩略图点击后自动滚动到侧边栏中心”的功能。

## 3. 环境配置

- [ ] **.env 检查**: 确认项目中的 `PDF_WORKER_URL` 是否需要根据环境变量切换内网地址。
