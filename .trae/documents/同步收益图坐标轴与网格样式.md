## 目标

- 让收益图（incomeChart）的 ECharts x/y 轴 label、分割线，以及 grid 背景与 K 线图保持一致。

## 对齐基准（来自 kChart）

- grid：`show: true`、`backgroundColor: '#F2F5FA'`、`borderWidth: 0`

- xAxis：`axisLine.lineStyle.color: '#DFE5F2'`、`axisLabel.color: '#777E8C'`、`axisLabel.rotate: 30`、`splitLine.show: true`、`splitLine.lineStyle.color: '#DFE5F2'`

- yAxis：`splitLine.show: true`、`splitLine.lineStyle.color: '#DFE5F2'`、`axisLabel.color: '#777E8C'`、`axisLine.lineStyle.color: '#DFE5F2'`

## 具体改动（incomeChart/index.tsx）

- 在 `option` 中补充/替换：

  - `grid`：保留现有 `left/right/top/bottom/containLabel`，新增/替换 `show/backgroundColor/borderWidth`

  - `xAxis`：

    - 替换 `axisLine.lineStyle.color` 为 `#DFE5F2`

    - 替换 `axisLabel.color` 为 `#777E8C`，保留现有 `rotate: 30`

    - 新增 `splitLine`（显示且颜色 `#DFE5F2`）

  - `yAxis`：

    - 保留 `formatter: '{value}%'`

    - 替换 `axisLabel.color` 为 `#777E8C`

    - 替换 `splitLine` 颜色为 `#DFE5F2` 并确保 `show: true`

    - 新增 `axisLine.lineStyle.color: '#DFE5F2'`

- 不改动 tooltip、legend、dataZoom、series 等业务逻辑，避免影响交互。

## 验证点

- 图表绘图区背景变为浅蓝灰（与 K 线一致）。

- x/y 轴文字颜色统一、分割线颜色统一，x 轴分割线可见。

- dataZoom slider 仍正常显示（保留原 bottom/height 配置）。
